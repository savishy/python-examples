# Warm-Up questions

## Even or odd
“Write a function that takes an integer n and returns true if it is even and false if it is odd. Do not use any built-in isEven helpers.”​

Solution: [evenOrOdd.py](evenOrOdd.py)

## Sum of an array
“Given an array of integers, write a function that returns the sum of all elements.”​

Solution: [sumOfArray.py](sumOfArray.py)

## Maximum element
“Given an array of integers, return the maximum value in the array. If the array is empty, decide how you want to handle it and explain your choice.”​

Solution: [maxElement.py](maxElement.py)

## Count vowels in a string

“Write a function that takes a lowercase English string and returns the number of vowels in it (a, e, i, o, u).”​

Solution: [countVowels.py](countVowels.py)

# Easy Questions

## Reverse an array in-place

“Given an array of integers, reverse the array in-place without using extra arrays.”​

## Reverse a string

“Given a string s, return a new string that is s reversed. Try to describe both an in-place and a not-in-place approach.”​

## Remove duplicates from an array

“Given an array of integers, return a new array that contains each distinct integer exactly once. The order does not matter.”​

## Second largest element

“Given an array of at least two integers, find the second largest element in a single pass if possible.”​

## Is palindrome (string)

“Write a function that checks whether a string is a palindrome (reads the same forward and backward), ignoring case and non-alphanumeric characters.”​

## Merge two sorted arrays

“You are given two sorted arrays of integers in non-decreasing order. Merge them into a single sorted array and return it.”​